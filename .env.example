# Root .env used by docker-compose
# Copy to .env and fill in your values

# Optional — if omitted, AI recommendations fall back to rule-based suggestions
ANTHROPIC_API_KEY=your_anthropic_api_key_here
